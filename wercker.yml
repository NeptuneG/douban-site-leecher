box: wercker/default

deploy:
  steps:
    - hotakasaito/elastic-beanstalk-deploy:
        key: $AWS_ACCESS_KEY_ID
        secret: $AWS_SECRET_KEY
        app_name: appname
        env_name: staging
        region: ap-northeast-1
